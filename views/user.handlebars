
<main>
    <h1>{{user.username}}</h1>

    {{#if loggedIn}}
    <div id="userArea"></div>
    {{/if}}
</main>

{{#if loggedIn}}
<script>
    var sessionUserId = {{userId}};
    var sessionUserData = {{{json user}}};
</script>
<script src="/js/userpage.js"></script>
{{/if}}