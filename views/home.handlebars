<main>
    {{#if loggedIn}}
    <input name="search" placeholder="search">
    <button id="submit">submit</button>

    {{#if areThereChats}}
    {{#each chats as |chat|}}
    <a href="/chat/{{chat.id}}">
        <h3>{{chat.title}}</h3>
    </a>
    {{/each}}

    {{else}}
    <h2>Nothing... yet</h2>
    {{/if}}

    {{else}}
    <h1>Empty... Login?</h1>
    {{/if}}
</main>
<footer>

</footer>
{{#if loggedIn}}
<script src="/js/home.js"></script>
{{/if}}